{{#each files}}
{{#if filename}}
### `{{filename}}`

{{/if}}
{{#each components}}
{{#if displayName}}**{{displayName}}** {{/if}}{{#if description}}{{description}}{{/if}}
{{#if props}}

Property | Type | Required | Description
:--- | :--- | :--- | :---
{{#each props}}
{{@key}}|{{#if type}}PropType {{type.name}}{{/if}}{{#if flowType}}FlowType {{flowType.name}}{{/if}}|{{#if required}}yes{{/if}}|{{description}}
{{/each}}
{{/if}}
{{/each}}

{{/each}}
